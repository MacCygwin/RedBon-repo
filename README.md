# RedBonOS Repository

* These contain some metapackages that I have added

## Add this into /etc/pacman.conf to download packages from repo

```
[redbon-repo]
SigLevel = Optional DatabaseOptional
Server = https://maccygwin.github.io/$repo/$arch
```
